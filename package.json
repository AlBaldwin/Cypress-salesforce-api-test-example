{
  "scripts": {"test-all" : "npx cypress run --reporter cypress-mochawesome-reporter",
  "test-ete": "npx cypress run ../ --reporter cypress-mochawesome-reporter"},
  "devDependencies": {
    "cypress": "^7.7.0",
    "cypress-mochawesome-reporter": "^2.2.0",
    "mocha": "^9.0.2",
    "mochawesome": "^6.2.2"
    
  }

}
